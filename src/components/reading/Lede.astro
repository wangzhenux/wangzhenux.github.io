---
---
<p class="lede"><slot /></p>
<style>
  /* ported from case-study-twilio.html .lede, tokenized.
     Fills body column; larger than body-text for emphasis. */
  .lede {
    font-family: var(--font-display);
    font-size: 22px;
    line-height: 1.55;
    color: var(--ink-primary);
    max-width: none;
    margin: var(--space-10) 0 0;
    padding: 0;
  }
</style>
