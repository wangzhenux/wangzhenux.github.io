---
import { getCollection } from 'astro:content';
interface Props { currentPath?: string; }
const { currentPath = '/' } = Astro.props;
// Cases flagged `navTab` are standalone sections (e.g. Sketches) and get their
// own top-nav link, placed between Writing and About.
const navTabs = (await getCollection('cases'))
  .filter((c) => c.data.navTab)
  .sort((a, b) => a.data.order - b.data.order)
  .map((c) => ({ href: `/work/${c.data.slug}`, label: c.data.navTab as string }));
const links = [
  { href: '/', label: 'Work' },
  { href: '/writing', label: 'Writing' },
  ...navTabs,
  { href: '/about', label: 'About' },
];
const isActive = (href: string) =>
  href === '/' ? currentPath === '/' : currentPath.startsWith(href);
---
<nav class="nav" aria-label="Primary">
  <a class="nav-name" href="/">Zhen Wang</a>
  <ul class="nav-links">
    {links.map((l) => (
      <li><a href={l.href} aria-current={isActive(l.href) ? 'page' : undefined}>{l.label}</a></li>
    ))}
  </ul>
</nav>
<style>
  /* Full-width bar: logo near the left gutter, links near the right gutter.
     Intentionally NOT constrained to the centered content frame. */
  .nav {
    display: flex; justify-content: space-between; align-items: baseline;
    padding: var(--space-5) var(--pad-page-x);
    border-bottom: 1px solid var(--rule);
  }
  .nav-name { font-family: var(--font-display); font-size: 18px; font-weight: 500; text-decoration: none; }
  .nav-links { display: flex; gap: var(--space-7); list-style: none; margin: 0; padding: 0; font-size: 13px; }
  .nav-links a { text-decoration: none; color: var(--ink-primary); }
  .nav-links a:hover { color: var(--accent); }
  .nav-links a[aria-current="page"] { color: var(--accent); }
  @media (max-width: 767px) { .nav-links { gap: var(--space-4); } }
</style>
